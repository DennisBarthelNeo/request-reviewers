{
  "name": "request-reviewers",
  "version": "1.0.0",
  "description": "Action to automatically request reviewers if the author is a member of any of the provided teams.",
  "main": "index.js",
  "scripts": {
    "bundle": "npm run format:write && npm run package",
    "format:write": "prettier --write **/*.ts",
    "package": "ncc build src/index.ts --license licenses.txt"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}
